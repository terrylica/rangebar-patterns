["release:full"]
description = "Run semantic-release (creates tag + GitHub release)"
run = "npm run release"

["release:dry"]
description = "Dry-run semantic-release (no publish)"
run = "npm run release:dry"
